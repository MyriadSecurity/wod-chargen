# Archived scripts

One-off migration and data-build scripts kept for reference. Not used in CI or runtime.

| Script | Purpose |
|--------|---------|
| `consolidate_archetypes.py` | Historical archetype layout consolidation |
| `migrate_archetype_layout.py` | Historical archetype directory migration |
| `build_loresheet_data.py` | One-time loresheet JSON builder from source tables |
